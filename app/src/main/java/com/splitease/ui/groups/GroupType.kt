package com.splitease.ui.groups

/**
 * Enumeration of group types for categorization.
 * Store as enum name (not ordinal) in database.
 */
enum class GroupType {
    TRIP,
    HOME,
    COUPLE,
    OTHER
}
