package com.splitease.data.local.entities

/**
 * Supported entity types for synchronization.
 * Enum constants must match the values stored in the database.
 */
enum class SyncEntityType {
    EXPENSE,
    GROUP,
    SETTLEMENT
}
